export function track(eventName, payload = {}) {
  // Stub analytics; can integrate with GA, Plausible, etc.
  // eslint-disable-next-line no-console
  console.log('[analytics]', eventName, payload);
}


